# M0 Test Report — Analysis and Scaffolding

**Date:** 2026-07-06/07 · **Commit:** (see git log `[M0]` series) · **Status: ACCEPTED — all gates green**

## Environment

- Host: Windows 11 (VS 17.14.30 installed but unused for driver build), WSL2 Ubuntu.
- Toolchain: **EWDK 26100.6584** (`ge_release_svc_prod1.26100.6584`) mounted from
  `C:\EWDK\ewdk_26100.6584.iso`; MSVC 14.44 BuildTools; WDK 10.0.26100.0; KMDF 1.35.
- Build entry: `./build.sh [Debug|Release] [--test]` (WSL) → stages to
  `%LOCALAPPDATA%\tt-win-kmd-build`, runs `build.ps1` in the EWDK environment.

## Gates run

| Gate | Command | Result |
|---|---|---|
| Compile+link /W4 + /WX (+msbuild /warnaserror) | `./build.sh Release` | **PASS** — 0 warnings, 0 errors |
| Code Analysis for Drivers (PREfast + drivers.dll, `DriverRecommendedRules.ruleset`) | `./build.sh Release --test` (forces `/t:Rebuild`) | **PASS** — 0 warnings, 0 errors |
| INF stamping (stampinf, KMDF 1.35) + Inf2Cat catalog | part of build | **PASS** — `ttkmd.cat` generated |
| Test signing (.sys + .cat, SHA256, WDKTestCert) | part of build | **PASS** — signability check clean |
| SDV | n/a | **NOT APPLICABLE** — SDV removed from WDK 26100 (DD-1); compensated by recommended-rules CA each `--test` build; CodeQL planned |
| Driver load/unload under Verifier (soft device) | `test/vm/guest/*.ps1` | **PASS** — see below |

## Artifacts

`out/Release/`: `ttkmd.sys`, `ttkmd.inf` (stamped), `ttkmd.cat` (signed), `ttkmd.pdb`, `tt-test.cer`.

## Analysis deliverable

- 14 subsystem sections in `docs/analysis/` (~6,000 lines), written one subsystem
  at a time; 77 open questions harvested to `docs/analysis/open-questions-raw.md`.
- Citation check and assembly of the master document
  (`docs/linux-driver-analysis.md`) — result recorded below when complete.

## VM load-test detail (2026-07-07)

- Guest: Windows 11 build 26200 under QEMU 8.2.2/KVM (WSL2), unattended install
  from `Win11_24H2_English_x64.iso`, test signing on, autologon `ttdev`.
- Driver package `ttkmd.inf` installed via pnputil (published as `oem2.inf`);
  soft device `ROOT\TTKMD_SOFT` created with devgen; device status **OK**,
  `ttkmd` service Running from the DriverStore.
- Driver Verifier: `verifier /flags 0x9BB /driver ttkmd.sys`
  (special pool, force IRQL, pool tracking, I/O, deadlock, DMA, security, misc);
  `verifier /query` confirmed `MODULE: ttkmd.sys` under verification post-reboot.
- `loadtest.ps1` result: `{"toggleCycles":20,"reinstallCycles":5,`
  `"verifierActive":true,"badEvents":[],"pass":true}` — no bugcheck, no WHEA,
  no Verifier violations across 20 disable/enable and 5 remove/reinstall cycles.
- Disk snapshot `m0-accepted` taken post-shutdown (`qemu-img snapshot -l`).

**M0 acceptance criteria: met.** (EWDK build clean at /W4 + warnings-as-errors ✓;
CA clean ✓; SDV n/a in this kit, documented DD-1 ✓; driver installs, starts,
loads/unloads without Verifier violations ✓; analysis + parity matrix delivered ✓.)

## Deviations / notes

- WDK VSIX 10.0.22621 rejected by VS 17.14 → EWDK pivot (DD-1 amendment).
- M0 acceptance criterion "driver loads and unloads without Verifier violations"
  remains open until the VM exists; everything scriptable in advance is committed
  under `test/vm/`.
